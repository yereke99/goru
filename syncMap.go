package main

/*
func main() {
	var sm = sync.Map{}

	sm.Store(1, "a")

	if v, ok := sm.Load(1); ok {
		fmt.Println(v)
	}

	if vv, ok := sm.LoadOrStore(2, "b"); !ok {
		fmt.Println(vv)
	}

	if vv, ok := sm.LoadOrStore(3, "c"); !ok {
		fmt.Println(vv)
	}

	sm.Range(func(k, v interface{}) bool {
		fmt.Print(k)
		fmt.Print(":")
		fmt.Print(v)
		fmt.Println()

		return true
	})
}
*/
/*
func main() {
	var sm = sync.Map{}

	sm.Store(1, "a")
	if v, ok := sm.Load(1); ok {
		fmt.Println(v)
	}

	if vv, ok := sm.LoadOrStore(1, "c"); !ok {
		fmt.Println(vv)
	}

	if vv, ok := sm.LoadOrStore(2, "c"); !ok {
		fmt.Println(vv)
	}
	sm.Range(func(k, v interface{}) bool {
		fmt.Print(k)
		fmt.Print(":")
		fmt.Print(v)
		fmt.Println()

		return true

	})
}
*/
